# arch-vm-firefox-repro-test
Pinned Arch Linux VM installer scripts for isolating the GPU as a variable when reproducing driver bugs by passing through different cards into an identical, version-locked guest.
